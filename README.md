<!-- 
1.git init
2.git remote add origin "github-path"
3.git add .
4.git commit -m "first commit"
5.git config --global user.email "your email"
git push(if error) => git push origin master
6.check the branch (main/master) => switched to main
7.git checkout -b main() 
8.git push origin main 
  run the steps from 3, 4 and 8
-->